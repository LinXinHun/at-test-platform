# 执行节点配置
server.port=8081

# 项目根目录配置（用于访问scripts）
project.root.dir=/Users/linxinhun/WorkSpace/at-test-platform

# 平台服务地址
platform.server.url=http://localhost:8080

# 节点信息
node.id=execution-node-1
node.name=Execution Node 1
node.host=localhost
node.port=8081
node.endpoint-type=MiniApp

# 心跳配置
heartbeat.interval=30000

# 临时文件配置
temp.script.directory=temp-scripts

# 日志配置
logging.level.com.testexecutionnode=INFO